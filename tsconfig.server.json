{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.server.tsbuildinfo",
    "target": "es2023",
    "lib": ["ES2023"],
    "module": "esnext",
    "types": ["node"],
    "skipLibCheck": true,

    /* Bundler mode — resolves the `.js` import specifiers to the `.ts` files,
       matching how tsx runs the server. */
    "moduleResolution": "bundler",
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,

    "strict": true,
    /* `field?: T` means "the key may be absent", not "the key may hold undefined".
       Without this, `{ work: undefined }` type-checks against `work?: Summary` and
       reaches Firestore, which rejects undefined at any depth (#1042). */
    "exactOptionalPropertyTypes": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["server/**/*.ts", "common/**/*.ts"],
  "exclude": ["server/**/*.spec.ts"]
}
