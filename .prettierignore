dist
node_modules
yarn.lock
*.md

# The shared-app wire contract. Generated by `JSON.stringify(_, null, 2)` in
# test/server/backends/appViewGolden.spec.ts, and copied byte-for-byte into
# mulmoserver `test/fixtures/sharedAppGolden/` — where a different prettier
# config would reformat them and make the two copies differ for no reason. A
# diff between them is supposed to mean something.
test/fixtures/sharedAppGolden/
